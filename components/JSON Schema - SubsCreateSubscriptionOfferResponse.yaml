{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "subsCreateSubscriptionOfferResponse",
	"description": "API used to assign an offer to an existing subscription, for example as part of the retention process. An offer can only be assigned to a subscription if the subscription is ACTIVE. If a specific date cancellation is registered on a future date, then the offer cannot be added either. When an offer is applied, an AMPS event will be triggered. An AMPS event is also triggered when rolling over from one plan to another. ",
	"type": "object",
	"properties": {
		"resultInfo": {
			"$ref": "#/definitions/resultInfoDetails"
		}
	},
	"required": [
		"resultInfo"
	],
	"additionalProperties": false,
	"definitions": {
		"resultInfoDetails": {
			"type": "object",
			"properties": {
				"resultCode": {
					"type": "integer"
				},
				"resultText": {
					"type": "string"
				}
			},
			"required": [
				"resultCode",
				"resultText"
			]
		},
		"solmTranslationGroup": {
			"type": "object",
			"properties": {
				"solmRefTransNo": {
					"type": "string"
				},
				"solmTranslationEntry": {
					"title": "solmTranslationEntry",
					"type": "array",
					"minItems": 1,
					"items": {
						"$ref": "#/definitions/solmTranslationEntry"
					}
				}
			},
			"required": [
				"solmRefTransNo",
				"solmTranslationEntry"
			],
			"additionalProperties": false
		},
		"solmTranslationEntry": {
			"type": "object",
			"properties": {
				"solmLocaleID": {
					"type": "string"
				},
				"solmRefTransText": {
					"type": "string"
				}
			},
			"required": [
				"solmLocaleID",
				"solmRefTransText"
			],
			"additionalProperties": false
		}
	}
}