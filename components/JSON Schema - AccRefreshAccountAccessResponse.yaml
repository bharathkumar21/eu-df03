{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "accRefreshAcctAccessResponse",
	"description": "AMPS API response structure for access list",
	"type": "object",
	"properties": {
		"resultInfo": {
			"$ref": "#/definitions/resultInfoDetails"
		}
	},
	"required": [
		"resultInfo"
	],
	"definitions": {
		"resultInfoDetails": {
			"type": "object",
			"properties": {
				"resultCode": {
					"type": "integer"
				},
				"resultText": {
					"type": "string"
				}
			},
			"required": [
				"resultCode",
				"resultText"
			]
		}
	}
}