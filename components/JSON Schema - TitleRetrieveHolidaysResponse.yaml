{
	"$schema": "http://json-schema.org/draft-04/schema#",
	"title": "titleRetrieveHolidaysResponse",
	"type": "object",
	"properties": {
		"resultInfo": {"$ref": "#/definitions/resultInfoDetails"},
		"titleRetrieveHolidaysList": {"title": "titleRetrieveHolidaysList", "type": "array","minItems": 1, "items": {"$ref": "#/definitions/titleRetrieveHolidaysInfo"}}
	},
	 "required": ["resultInfo", "titleRetrieveHolidaysList"],
	 "additionalProperties": false,

	"definitions": {
		"resultInfoDetails": {"type": "object",
			"properties": {
				"resultCode": {"type": "integer"},
				"resultText": {"type": "string"}
			},
			"required": ["resultCode","resultText"],
			"additionalProperties": false
		},
		
		"titleRetrieveHolidaysInfo": {"type": "object",
			"properties": {
				"countryCode": {"type": "string", "maxLength": 2},
				"holidayDate": {"type": "string", "maxLength": 10},
				"holidayName": {"type": "string", "maxLength": 100},
				"holidayOneLiner": {"type": "string", "maxLength": 1000},
				"holidayType": {"type": "string", "maxLength": 10, "enum": ["NATIONAL", "STATE", "REGIONAL", "LOCAL"]}
			},
			"required": ["countryCode", "holidayDate", "holidayName", "holidayOneLiner", "holidayType"], 
			"additionalProperties": false
		}
	}
}